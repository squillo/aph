{
  "title": "Extend a Guardrail — Tighten a Confidence Floor",
  "tags": [
    "overlay",
    "extension",
    "tighten-only",
    "guardrails"
  ],
  "nlang_code": "// Base — excerpted VERBATIM from the shipped family. Its real floors are\n// t0_signals >= 970, t1_embedding >= 930, t3_local_llm >= 900.\nclassifiers \"APH_ACT_DATA_MUTATION\" {\n  labels {\n    RECORD_HARD_DELETE {\n      description = \"Irreversibly destroys a single record, including any recoverable copy the system holds, such that the prior value cannot be produced again. Erasure requests, purges, and 'permanently, no backup' deletions land here. This label asserts irreversibility; when the act's reversibility is genuinely undetermined, the classifier must abstain rather than choose between this label and RECORD_SOFT_DELETE.\"\n      examples    = [\"Purge the applicant's file permanently — this is a GDPR erasure request.\",\n                     \"Delete ledger row 4471 for good; no backup copy of it should survive anywhere.\",\n                     \"Wipe the test tenant's record completely so that it cannot be restored later.\"]\n    }\n  }\n}\n\n// Overlay — an operator in a regulated environment demands more confidence\n// before this classifier commits to a label. Raising a floor is legal.\nclassifiers \"APH_ACT_DATA_MUTATION\" {\n  escalate t0_signals   >= 990\n  escalate t1_embedding >= 960\n}",
  "note": "An overlay may RAISE a confidence floor, ADD a label, REMOVE a rung, RESTRICT privacy locality, REDUCE a quota, or upgrade a fail-open posture to fail-closed. It may never widen. Lowering a floor, adding a rung, relaxing privacy, and relaxing fail posture are each a distinct, separately-numbered load refusal — and they are refusals, not silent clamps, because a guardrail that quietly accepted a weakening would be worse than no guardrail at all: the operator would believe a gate was in place that had in fact been removed. The direction of the lattice is the whole safety argument. A contributor can only ever make this vocabulary stricter than the author shipped it, which is what makes it safe to resolve a vocabulary you did not write.",
  "book_ref": "APH Guardrails: extension lattice"
}
