{
  "title": "Credential Subject",
  "tags": [
    "envelope",
    "claim",
    "subject",
    "policy",
    "authorization"
  ],
  "nlang_code": "mod blocks CredentialSubject {\n  props {\n    human_principal: HumanPrincipalRef,\n    agent: AgentRef,\n    channel: ChannelDescriptor,\n    communication: CommunicationDescriptor,\n    policy: PolicyDescriptor,\n    notarization: NotarizationMetadata,\n    #[optional(true)]\n    apple_aur_acceptance: AppleAurAcceptanceClaim,\n  }\n}",
  "note": "The notarized claim: who authorized what, on which channel, under which policy, attested by which notary. The six required objects answer those questions in order; `apple_aur_acceptance` is a registered optional extension and is omitted when absent, so extension-free envelopes stay byte-identical to pre-extension ones.",
  "book_ref": "APH Spec: \u00a77.1.2 CredentialSubject"
}
